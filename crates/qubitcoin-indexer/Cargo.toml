[package]
name = "qubitcoin-indexer"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "In-process WASM secondary indexer runtime for Qubitcoin"

[dependencies]
qubitcoin-indexer-core = { workspace = true }
rocksdb = { workspace = true }
tracing = { workspace = true }
parking_lot = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
rayon = { workspace = true }
tokio = { workspace = true }
wasmtime = "43"
prost = "0.12"
sha2 = "0.10"
toml = "0.8"
# Metashrew runtime — the production WASM indexer engine.
# Using the real metashrew crates ensures exact storage format
# compatibility with the alkanes.wasm and other indexer modules.
metashrew-runtime = { git = "https://github.com/kungfuflex/metashrew", branch = "develop" }
rockshrew-runtime = { git = "https://github.com/kungfuflex/metashrew", branch = "develop" }
metashrew-sync = { git = "https://github.com/kungfuflex/metashrew", branch = "develop" }
async-trait = "0.1"

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true }
wat = "1.0"

[build-dependencies]
prost-build = "0.12"
